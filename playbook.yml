---

- hosts: kvm-tomcat
  tasks:
    - name: Copy war file
      copy:
        src: "{{ item }}"
        dest: /opt/tomcat/latest/webapps/
        owner: tomcat
        group: tomcat
        mode: '0644'
      with_fileglob: "target/*.war"
